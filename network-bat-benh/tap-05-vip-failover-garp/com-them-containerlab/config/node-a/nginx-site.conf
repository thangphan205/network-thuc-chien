server {
    listen 80 default_server;
    root /var/www/html;
    index index.html;

    # Header định danh backend -> monitor.sh trên client đọc header này
    # để biết VIP đang được node nào phục vụ.
    add_header X-Backend-Server "node-a-MASTER" always;
}
